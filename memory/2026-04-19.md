# 2026-04-19 日志

## 今日任务

### 抖音指数日报 (08:00)
- **状态**: ⚠️ 部分成功
- **数据日期**: 2026-04-15（抖音每周更新，4/19为周日无新数据）
- **发送到**: oc_2581c03b79e4893cc3616b253d60f34e
- **message_id**: om_x100b5175afa068a4c245ff28c585f7e

**技术问题**:
1. 脚本v9使用 `launch_persistent_context` 时报 `port` 参数错误（Playwright 1.58.0不支持）
2. 修复后CDP连接失败（Chrome未在9222端口运行）
3. 尝试连接OpenClaw Chrome（18800端口）成功
4. `inner_text('body')` 返回截断内容，改用 `document.body.innerText` JavaScript获取完整文本

**脚本修复**:
- 移除 `launch_persistent_context` 的 `port=DEBUG_PORT` 参数
- 添加 OpenClaw Chrome（18800端口）作为CDP连接备选
- 将 `inner_text('body')` 改为 `page.evaluate("() => document.body.innerText")`

**数据**（2026-04-15）:
| 景区 | 搜索指数 | 搜索日环比 | 综合指数 | 综合日环比 |
|------|---------|---------|---------|---------|
| 万岁山武侠城 | 53,275 | -1.05% | 24,427 | -0.40% |
| 清明上河园 | 174,087 | -11.46% | 59,634 | -8.78% |
| 郑州银基动物王国 | 28,447 | 5.41% | 8,752 | 5.19% |
| 郑州方特欢乐世界 | 7,875 | 10.57% | 2,580 | 4.62% |
| 郑州海昌海洋公园 | 4,947 | -4.79% | 1,742 | -1.53% |
| 建业电影小镇 | 4,925 | 14.32% | 2,099 | 27.21% |
| 只有河南戏剧幻城 | 3,116 | -14.68% | 1,871 | -3.06% |
| 只有红楼梦戏剧幻城 | 2,799 | -8.68% | 1,311 | 22.52% |

**建业电影小镇亮点**: 双指标增速第一（搜索+14.32%，综合+27.21%）🔥
---

## 飞书卡片格式问题（10:00-11:00）

**问题**：飞书 Webhook Token 失效导致 card 发送失败。重建发送逻辑时，format 走偏（表格被拆散、字符乱码）。

**根因**：OpenClaw `message(action=send)` 的 `card` 参数格式与飞书 IM API 不兼容，导致 400 报错。

**解决方案**：
- 新建独立发送脚本 `scripts/send_feishu_card.py`（直调飞书 API，绕开 OpenClaw）
- 测试私信成功（message_id: om_x100b5170708450a0c2493dbb75d206c）
- `tag: "markdown"` 内嵌管道符表格可行（飞书渲染正常）

**今日确认的日报格式**：
- 5列：景区 | 搜索指数 | 搜索日环比 | 综合指数 | 综合日环比
- **按综合指数降序排列**
- **电影小镇整行加粗 + 🔺标记**
- 下方：🔥亮点 + 💡洞察
- 用 `scripts/send_feishu_card.py` 发送，不用 OpenClaw 内置 card

**已解决**：
- 确认早上那张是直调API发送的普通interactive卡片（tag:img是API返回格式，非渲染差异）
- 正确格式：一个`tag:"markdown"`元素含表头+表格，↓↑符号，电影小镇加粗
- 完整日报包含：表格+亮点+洞察三个分段markdown
- 已发送到群（message_id: om_x100b5170312500a0c220fc6a198917c）

**完整日报模板已确认（04-19）**：4部分 - 竞品表格+深度分析+竞品格局+行动建议

**发送脚本**：`scripts/send_feishu_card.py`（已验证有效）
- 根级`header`对象（不是body.header）
- 根级`elements[]`数组（不是body.elements）
- 发送到群：receive_id_type=chat_id
- 发给用户：receive_id_type=open_id

---

## 发送目标群 ID（更新）

- 电影小镇群：`oc_2581c03b79e4893cc3616b253d60f34e`
- 数据推送群：`oc_f109bcfd1bc7e166fd0ae077f70247cf`
- 用户个人：直接发私信（chat_id = sender_id）